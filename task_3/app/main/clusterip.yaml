apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: my-app
spec:
  ports:
  - name: http-metrics
    protocol: TCP
    port: 80
    targetPort: 3000
  selector:
    app: my-app
  type: ClusterIP